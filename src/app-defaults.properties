# Demo defaults embedded in the JAR for plug-and-play startup.
# Environment variables and local .env override these values.
DB_URL=jdbc:mysql://13.60.24.212:3306/billeterie?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=billeterie_app
DB_PASSWORD=MotDePasseFort!

# If SMTP is not configured, skip OTP to keep demo login functional.
OTP_BYPASS_WITHOUT_SMTP=true
